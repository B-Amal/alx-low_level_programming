#include <stdio.h>
/**
 * main - The program prints a string using the function printf.
 *
 * Return: 0 on success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
