#include "main.h"

/**
 * add - The function adds two integers and returns the result.
 * @x: First number.
 * @y: Second number.
 * Return: The sum of the two numbers.
 */
int add(int x, int y)
{
	return (x + y);
}
